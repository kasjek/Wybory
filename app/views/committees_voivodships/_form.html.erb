<%= form_for(@committees_voivodship) do |f| %>
  <% if @committees_voivodship.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@committees_voivodship.errors.count, "error") %> prohibited this committees_voivodship from being saved:</h2>

      <ul>
      <% @committees_voivodship.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :committee_id %><br>
    <%= f.number_field :committee_id %>
  </div>
  <div class="field">
    <%= f.label :voivodship_id %><br>
    <%= f.number_field :voivodship_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
