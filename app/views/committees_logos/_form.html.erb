<%= form_for(@committees_logo) do |f| %>
  <% if @committees_logo.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@committees_logo.errors.count, "error") %> prohibited this committees_logo from being saved:</h2>

      <ul>
      <% @committees_logo.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :committee_id %><br>
    <%= f.number_field :committee_id %>
  </div>
  <div class="field">
    <%= f.label :logo_id %><br>
    <%= f.number_field :logo_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
